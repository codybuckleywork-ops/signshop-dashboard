# local data — never commit the shop's live database or settings
dashboard.db
config.json
tokens.json

# python
__pycache__/
*.pyc
.venv/
venv/

# os / editor
.DS_Store
Thumbs.db

# local data — backup zips and customer job files stay off GitHub
backups/
uploads/
